import React from 'react'

const NotFoundPage = () => (
  <div>
    <h1>NOT FOUND</h1>
    <p>The page that you are trying to reach cannot be found. It may have been moved or no longer exists.</p>
  </div>
)

export default NotFoundPage
